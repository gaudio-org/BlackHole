#!/usr/bin/env zsh

driverName="JustVoice"

sudo rm -rf /Library/Audio/Plug-Ins/HAL/${driverName}.driver
sudo cp -R Installer/root/${driverName}.driver /Library/Audio/Plug-Ins/HAL
rm -rf Installer/root/${driverName}.driver

sudo launchctl kickstart -kp system/com.apple.audio.coreaudiod
